{
  "name": "pre-convert-demo",
  "private": true,
  "license": "MIT",
  "scripts": {
    "up": "up +D",
    "dev": "vite",
    "format": "prettier --write **/*.{js,css,yml,html}",
    "build": "rimraf docs && vite build && move-file dist docs && echo keepme > docs/.nojekyll"
  },
  "dependencies": {
    "tachyons": "^4.12.0",
    "tiny-oss": "^0.5.0",
    "uuid": "^8.3.1"
  },
  "devDependencies": {
    "@hyrious/up": "^0.1.1",
    "move-file-cli": "^2.0.0",
    "rimraf": "^3.0.2",
    "vite": "^1.0.0-rc.9",
    "vite-plugin-yaml": "^1.0.4"
  }
}
